<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Borde -->
    <stroke
        android:width="1dp"
        android:color="#0000FF00" /> <!-- Color del borde (negro) -->

    <!-- Relleno del fondo -->
    <solid android:color="@android:color/white" /> <!-- Color de fondo (blanco) -->

    <!-- Esquinas redondeadas -->
    <corners android:radius="4dp" /> <!-- Radio de las esquinas -->
</shape>
